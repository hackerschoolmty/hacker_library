<div id="picture_<%= picture.id %>" class="small-4 columns">
  <%= image_tag(picture.image_url(:thumb), class: "thumbnail") %>
  <br/>
  
	<%= link_to "Eliminar", picture_path(picture), method: :delete, data: {confirm: "¿estás seguro?"}, remote: true %> 
  
</div>